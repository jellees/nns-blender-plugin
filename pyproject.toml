[project]
name = "nns_plugin"
requires-python = ">=3.11"
version = "0.0.1"

dependencies = [
    "pytest",
    "pytest-cov",
    "coverage",
    "bpy",
]

[tool.pytest.ini_options]
pythonpath = [
  ".", "src",
]
